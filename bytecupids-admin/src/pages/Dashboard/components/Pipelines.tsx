import type React from "react";

const Pipelines : React.FC = () => {
    return (
        <div>
            <h1>Pipelines</h1>
            <p>Manage your pipelines here.</p>
        </div>
    );
}
export default Pipelines;